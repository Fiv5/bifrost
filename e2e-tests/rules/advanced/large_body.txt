# 大文件 Body 处理保护策略测试
# 测试请求和响应 body 在超过 max_body_buffer_size 时的行为
# 验证"流式转发 + 按需缓冲 + 大小保护"策略

# ============================================================
# 请求 body 规则测试
# ============================================================

# LB-01 请求 body 替换规则 - 用于测试小/大请求 body 场景
test-large-req-replace.local http://127.0.0.1:3000 reqReplace://REQ_MARKER_12345=REQ_REPLACED

# ============================================================
# 响应 body 规则测试
# ============================================================

# LB-02 响应 body 替换规则 - 用于测试小/大响应 body 场景
test-large-res-replace.local http://127.0.0.1:3000 resReplace://RES_MARKER_67890=RES_REPLACED

# ============================================================
# 请求 + 响应 body 规则测试
# ============================================================

# LB-03 同时有请求和响应 body 规则
test-large-both-replace.local http://127.0.0.1:3000 reqReplace://REQ_MARKER_12345=REQ_REPLACED resReplace://RES_MARKER_67890=RES_REPLACED

# ============================================================
# 无 body 规则基准测试
# ============================================================

# LB-04 无 body 规则 - 仅转发，用于验证大数据流式转发
test-large-no-rule.local http://127.0.0.1:3000
