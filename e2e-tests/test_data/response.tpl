{
  "template": true,
  "requestId": "${reqId}",
  "method": "${method}",
  "url": "${url}",
  "host": "${host}",
  "path": "${pathname}",
  "query": "${search}",
  "clientIp": "${clientIp}",
  "timestamp": "${now}",
  "randomValue": "${random}"
}
